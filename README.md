# How we got here

1. Run `npm create vite@latest . -- --template react` in an empty folder
1. `npm install`
1. `npm run dev`
1. Modify `src/App.jsx` to our template, and empty `src/index.css` and remove `src/App.css`)
